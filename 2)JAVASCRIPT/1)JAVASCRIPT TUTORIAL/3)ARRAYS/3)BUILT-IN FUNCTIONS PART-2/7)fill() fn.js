
// fill=>
//The fill function changes all elements in an array with a static value,
//starting from a specified start index and ending at a specified end index.

const numbers = [1, 2, 3, 4, 5,6];
numbers.fill(0, 2, 4);
console.log(numbers);
 // Outputs: [1, 2, 0, 0, 5]
